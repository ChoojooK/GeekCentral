/*
Logical operators in C++ are used to combine or invert boolean expressions.
They help in making complex decisions by evaluating multiple conditions.
*/

bool a = true, b = false;
bool resultAnd = (a && b);  // Logical AND
bool resultOr = (a || b);  // Logical OR
bool resultNotA = !a;  // Logical NOT
